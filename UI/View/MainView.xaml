<Window x:Class="UI.View.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UI.View"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Style="{StaticResource MaterialDesignWindow}"
        mc:Ignorable="d"
        Title="MainView" Height="600" Width="900">
    
    
    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="0.1*"></RowDefinition>
        </Grid.RowDefinitions>
        <TabControl SelectedIndex="{Binding }"
                    materialDesign:NavigationRailAssist.ShowSelectionBackground="True"
                    SnapsToDevicePixels="True"
                    Style="{StaticResource MaterialDesignNavigationRailTabControl}"
                    TabStripPlacement="Left">
            <TabItem>
                <TabItem.Header>
                    <StackPanel
                        Width="auto"
                        Height="auto">
                        <materialDesign:PackIcon
                            Width="24"
                            Height="24"
                            HorizontalAlignment="Center"
                            Kind="Bitcoin" />
                        <TextBlock
                            HorizontalAlignment="Center"
                            Text="Top Coins" />
                    </StackPanel>
                </TabItem.Header>

               
                <StackPanel Orientation="Vertical">
                   
                    <Grid HorizontalAlignment="Stretch" Margin="10">

                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <TextBox Grid.Column="0" 
                                 Height="30" 
                                 VerticalAlignment="Center" 
                                 Text="{Binding }" />

                        <Button Grid.Column="1" 
                                Content="Search" 
                                Command="{Binding}" 
                                Height="30" 
                                Margin="10,0,0,0" />
                    </Grid>

                    <DataGrid ItemsSource="{Binding }" 
                              AutoGenerateColumns="False" 
                              Margin="0"
                              CanUserResizeColumns="True">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="ID" Binding="{Binding}"/>
                            <DataGridTextColumn Header="Symbol" Binding="{Binding }"/>
                            <DataGridTextColumn Header="Name" Binding="{Binding }" />
                            <DataGridTextColumn Header="Current Price" Binding="{Binding }"/>
                            <DataGridTextColumn Header="Market Cap" Binding="{Binding }"/>
                            <DataGridTextColumn Header="24h Volume" Binding="{Binding }"/>
                            <DataGridTextColumn Header="Change (24h)" Binding="{Binding }" />
                            <DataGridTextColumn Header="Circulating Supply" Binding="{Binding }"/>
                            <DataGridTextColumn Header="Last Updated" Binding="{Binding }" />
                        </DataGrid.Columns>
                    </DataGrid>
                </StackPanel>

                

            </TabItem>
            
            <TabItem>
                <TabItem.Header>
                    <StackPanel
                        Width="auto"
                        Height="auto">
                        <materialDesign:PackIcon
                            Width="24"
                            Height="24"
                            HorizontalAlignment="Center"
                            Kind="Coins" />
                        <TextBlock
                            HorizontalAlignment="Center"
                            Text="Coin Info" />
                    </StackPanel>
                </TabItem.Header>
            </TabItem>

            <TabItem>
                <TabItem.Header>
                    <StackPanel
                        Width="auto"
                        Height="auto">
                        <materialDesign:PackIcon
                            Width="24"
                            Height="24"
                            HorizontalAlignment="Center"
                            Kind="Settings" />
                        <TextBlock
                            HorizontalAlignment="Center"
                            Text="Settings" />
                    </StackPanel>
                </TabItem.Header>

                <StackPanel Margin="10,0,0,0">
                    <!-- Toggle Dark/Light Theme Mode=TwoWay-->
                    <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                        <TextBlock Text="Light Theme:" VerticalAlignment="Center" Margin="0,0,10,0"/>

                        <ToggleButton
                            Content="{materialDesign:PackIcon Kind=SunMoonStars,Size=32}"
                            IsChecked="{Binding}"
                            Style="{StaticResource MaterialDesignFlatPrimaryToggleButton}"
                            ToolTip="Toggle Dark/Light Theme"
                        />
                    </StackPanel>

                    <!-- Language Changer  Mode=TwoWay-->
                    <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                        <TextBlock Text="Language:" VerticalAlignment="Center" />
                        <ComboBox Width="150" 
                                  SelectedItem="{Binding }" 
                                  ItemsSource="{Binding }"
                                  Margin="10,0,0,0"/>
                    </StackPanel>

                    <!-- Top N Currency Mode=TwoWay -->
                    <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                        <TextBlock Text="Top Currencies Count:" VerticalAlignment="Center" />
                        <ComboBox Width="150" 
                                  SelectedItem="{Binding }" 
                                  ItemsSource="{Binding }"
                                  Margin="10,0,0,0"/>
                    </StackPanel>

                    <!-- Save Changes Button -->
                    <Button Content="Save Settings" Command="{Binding SaveSettingsCommand}" Margin="200,25,200,0"/>
                </StackPanel>


            </TabItem>

        </TabControl>
    </Grid>
</Window>
